<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.temple.gtc_core" >

    <application
        android:allowBackup="true"  >

        <!-- <meta-data
            android:name="com.google.android.gms.version"
            android:value="11010000" /> -->

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <service android:name=".services.ResourceLoggerService"
            android:process=":ResourceLoggerService" >
        </service>

        <service android:name=".services.FrameLoggerService"
            android:process=":FrameLoggerService" >
        </service>

    </application>

</manifest>
